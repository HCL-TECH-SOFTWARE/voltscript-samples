meta {
  name: getDocuments
  type: http
  seq: 13
}

post {
  url: {{serverUrl}}/bulk/unid?dataSource={{scope}}
  body: json
  auth: inherit
}

params:query {
  dataSource: {{scope}}
}

headers {
  accept: application/json
  Content-Type: application/json
}

script:pre-request {
  const jsonObj = {};
  jsonObj.unids = bru.getEnvVar("unids");
  req.setBody(jsonObj);
}
